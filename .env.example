# Azure OpenAI Configuration
ENDPOINT_URL=https://your-azure-openai-instance.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-key
DEPLOYMENT_NAME=your-gpt-model-deployment-name

# Remote MCP Server Configuration (Production Infrastructure)
# The MCP server is deployed separately on Azure infrastructure
# Update this URL to point to your deployed MCP server
MCP_SERVER_URL=https://your-mcp-server.azurecontainerapps.io
MCP_API_KEY=your-secure-api-key-here

# Note: The CLI assistant is a pure client that connects to the remote MCP server
# Azure authentication for security tools is configured on the MCP server side
# The assistant only needs the MCP server URL and API key to connect
